/**
 * The shared contract between the backend and the frontend.
 *
 * Both sides import from here, so "what a phone number is" cannot drift apart
 * between the form that collects it and the schema that stores it.
 */
export * from './fieldTypes';
export * from './parseBody';
export * from './locations';
//# sourceMappingURL=index.d.ts.map